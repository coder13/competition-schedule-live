type Subscription {
  activities(competitionIds: [String!]!, roomId: Int): [Activity]
  activityStarted(competitionId: String!, roomId: Int): Activity
  activityStopped(competitionId: String!, roomId: Int): Activity
  activityUpdated(competitionIds: [String!], roomId: Int): Activity
}
